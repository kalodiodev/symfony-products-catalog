<form style="float: right; margin-top: 15px;" action="{{ url('admin_brands_delete', {id: brand.id}) }}" method="post">
    <input type="hidden" name="token" value="{{ csrf_token('delete') }}" />
    <button type="submit" class="btn btn-danger">{{ 'admin.brands.button.delete'|trans }}</button>
</form>