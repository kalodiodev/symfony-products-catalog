<form style="float: right; margin-top: 15px;" action="{{ url('admin_products_delete', {id: product.id}) }}" method="post">
    <input type="hidden" name="token" value="{{ csrf_token('delete') }}" />
    <button type="submit" class="btn btn-danger">{{ 'admin.products.button.delete'|trans }}</button>
</form>