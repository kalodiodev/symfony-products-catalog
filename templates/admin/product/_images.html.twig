<h3>{{ 'admin.products.show.subtitle.images'|trans }}</h3>

<div class="row">
    {% for image in product.images %}
        <div class="col-md-3">
            <img src="{{ image.path }}" alt="" >
        </div>
    {% endfor %}
</div>

{{ form(imgForm) }}